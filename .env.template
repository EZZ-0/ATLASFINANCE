# ⚠️ TEMPLATE FOR .env FILE
# File: .env (create this in the project root folder)
# DO NOT commit .env to git - it should be in .gitignore

# AI Configuration (REQUIRED)
GEMINI_API_KEY=your_google_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash-exp

# Analytics
ENABLE_ANALYTICS=true

# Optional: Ollama (if installed locally)
# OLLAMA_URL=http://localhost:11434/api/generate
# OLLAMA_MODEL=llama3.1

# Optional: Groq (if you want to add it later)
# GROQ_API_KEY=your_groq_key_here

# INSTRUCTIONS:
# 1. Copy this entire file to a new file named ".env" (no .txt extension)
# 2. Replace "your_google_gemini_api_key_here" with your actual API key
# 3. Get your API key from: https://console.cloud.google.com/apis/credentials
# 4. NEVER commit the .env file to git!
